{
  "nativeMCPServers": [],
  "mcpServers": {
    "github": {
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_ACCESS_TOKEN_MCPHUB}"
      },
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITHUB_PERSONAL_ACCESS_TOKEN",
        "ghcr.io/github/github-mcp-server"
      ],
      "custom_instructions": {
        "text": "owner is wchavarria03"
      },
      "name": "github",
      "disabled": false,
      "disabled_tools": ["manage_notification_subscription"],
      "command": "docker"
    }
  }
}
